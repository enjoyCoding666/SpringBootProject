<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.OrderTestMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.example.demo.domain.OrderTestEntity">
        <id column="id" property="id"/>
        <result column="order_id" property="orderId"/>
        <result column="pay_status" property="payStatus"/>
        <result column="user_id" property="userId"/>
        <result column="total_price" property="totalPrice"/>
        <result column="result" property="result"/>
        <result column="order_desc" property="orderDesc"/>
        <result column="order_date" property="orderDate"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="is_delete" property="isDelete"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id
        , order_id, pay_status, user_id, total_price, result, order_desc, order_date, create_time, update_time, is_delete
    </sql>

</mapper>
