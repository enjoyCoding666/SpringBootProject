dubbo:
  application:
    # 应用名称
    name: dubbo-consumer
  protocol:
    # 协议名称
    name: dubbo
    # 协议端口
    port: 20881
  registry:
    # 注册中心地址
    address: zookeeper://127.0.0.1:2181

server:
  # 服务器端口号
  port: 8082
